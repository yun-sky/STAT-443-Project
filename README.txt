`data_cleaning_and_dependencies.R` automatically reads the datasets
from the github repository. If you'd like to read them locally, 
replace file links in the R file mentioned above with the CSV
files in the data folder. 

